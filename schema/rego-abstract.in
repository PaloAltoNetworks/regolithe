{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "title": "Abstract",
    "description": "An abstract allows to declare a set of base attributes and/or relations that can be reused into other specifications",
    "type": "object",
    "additionalProperties": false,

    "properties": {
        "attributes": {
            "$ref": "#/definitions/attributes"
        },
        "indexes": {
            "$ref": "#/definitions/indexes"
        }
    },
    "definitions": {
        "indexes": {
            "title": "Indexes",
            "description": "Describes the indexes of the spec.",
            "additionalProperties": false,
            "type": "array",
            "items": {
                "type": "array",
                "items": {
                    "type": "string"
                }
            }
        },
        "attributes": {
__ATTRIBUTE__
        }
    }
}
